# Copy to terraform.tfvars and fill in real values (never commit terraform.tfvars)

do_token            = "dop_v1_..."
ssh_key_fingerprint = "aa:bb:cc:..."       # from: doctl compute ssh-key list

secret_key          = ""                   # openssl rand -base64 32
region              = "nyc3"
droplet_size        = "s-1vcpu-1gb"

domain              = "localisprod.com"    # must have an A record pointing to the droplet IP
acme_email          = "you@example.com"    # Let's Encrypt expiry notifications
