# Copy to .env.test and fill in real values (never commit .env.test)

# Production app URL
BASE_URL=https://localisprod.com

# JWT secret — must match JWT_SECRET in /opt/localisprod/app/.env on the server
# Used by auth:ssh to mint tokens without a browser
JWT_SECRET=XsKJfrlSm5BtMsgRKiTRrfXewEyTLRBfJCc1/h9jMuw=

# SSH credentials for the test node (production server used as deployment target)
TEST_NODE_HOST=167.71.230.5
TEST_NODE_USER=root
TEST_NODE_PORT=22

# SSH private key: provide EITHER a file path OR base64-encoded content
TEST_NODE_KEY_PATH=~/.ssh/digitalocean
# TEST_NODE_KEY_B64=<cat ~/.ssh/digitalocean | base64 | tr -d '\n'>

# Password for the test postgres database spun up during tests
TEST_DB_PASSWORD=CiTestSecure999!
