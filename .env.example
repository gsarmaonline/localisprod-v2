# .env.example
# Copy this file to .env and fill in your actual values:
#   cp .env.example .env
#
# DO NOT commit .env to version control — it contains secrets!
# The server loads .env automatically on startup.

# =============================================================================
# Application
# =============================================================================

# HTTP port to listen on (optional, default: 8080)
PORT=8080

# Public URL of the application, used for OAuth redirect URIs.
# Include protocol; no trailing slash. (optional, default: http://localhost:PORT)
APP_URL=http://localhost:8080

# =============================================================================
# Storage
# =============================================================================

# Path to the SQLite database file. (optional, default: cluster.db)
DB_PATH=cluster.db

# =============================================================================
# Authentication & Security
# =============================================================================

# JWT signing secret. REQUIRED.
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# Encryption key for SSH credentials stored at rest. REQUIRED.
# Must be a base64-encoded 32-byte key.
# Generate with: openssl rand -base64 32
SECRET_KEY=your-base64-encoded-32-byte-key-here

# =============================================================================
# Google OAuth
# =============================================================================
# REQUIRED. Create credentials at: https://console.cloud.google.com/apis/credentials
# Set the authorised redirect URI to: <APP_URL>/api/auth/google/callback

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
